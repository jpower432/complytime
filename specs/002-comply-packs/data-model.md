# Data Model: Comply-Packs

**Branch**: `002-comply-packs` | **Date**: 2026-02-27

## Entities

### PackArtifact (OCI)

OCI artifact assembled by `complypack build` and distributed via OCI registries. Single OCI manifest with typed layers, identified by custom media types.

| Field | Type | Description |
|:---|:---|:---|
| `config` | OCI descriptor | Empty config blob (`application/vnd.oci.empty.v1+json`) |
| `layers` | `[]OCI descriptor` | Typed layers: manifest YAML, example config, provider binaries, policy OCI layouts |
| `annotations` | `map[string]string` | `org.opencontainers.image.source`, `org.complytime.pack.id`, `org.complytime.pack.version` |

**Layer Types**:

| Media Type | Content | Annotation (`image.title`) |
|:---|:---|:---|
| `application/vnd.complytime.pack.manifest.v1+yaml` | `complypack.yaml` | `complypack.yaml` |
| `application/vnd.complytime.pack.config.v1+yaml` | `complytime.yaml.example` | `complytime.yaml.example` |
| `application/vnd.complytime.pack.provider.v1` | Provider executable | `bin/{binary-name}` |
| `application/vnd.complytime.pack.policy-layout.v1.tar+gzip` | OCI Layout dir (tar.gz) | `policies/{policy-id}.tar.gz` |

**Source**: New — `internal/pack/artifact.go`
**Validation**: At least one provider layer, at least one policy layout layer, exactly one manifest layer, exactly one config layer.
**Media type constants**: `internal/complytime/consts.go` (new pack-specific constants)

---

### PackManifest (updated — multi-registry)

Pack manifest file (YAML). Defined in `internal/complytime/pack.go`. Top-level `registry` field removed — each `PackPolicyEntry` carries its own `url` (full OCI reference), enabling packs that pull policies from multiple registries.

| Field | Type | Required | Description |
|:---|:---|:---|:---|
| `id` | `string` | Yes | Pack identifier |
| `version` | `string` | Yes | Semantic version |
| `description` | `string` | No | Human-readable description |
| `platform` | `PlatformConfig` | Yes | Target `os`/`arch` (single-platform per artifact) and datastream path |
| `policies` | `[]PackPolicyEntry` | Yes (>=1) | Policies to bundle (each with its own OCI URL) |
| `providers` | `[]PackProviderEntry` | Yes (>=1) | Provider binaries to bundle |
| `system-dependencies` | `[]SystemDependency` | No | OS packages required at runtime |

**Source**: `internal/complytime/pack.go` — `PackManifest` struct
**Relationship**: `complypack build` reads this → fetches policies from their respective registries → assembles `PackArtifact` → pushes to registry. `complyctl pack install` extracts this from `PackArtifact` → writes to workspace.
**Schema change**: `RegistryConfig` type removed from `config.go`. No longer used by any struct.

**Sub-entity: PlatformConfig** (add `arch` field):

| Field | Type | Required | Description |
|:---|:---|:---|:---|
| `os` | `string` | Yes | Target operating system (e.g., `linux`, `darwin`) |
| `arch` | `string` | Yes | Target architecture (e.g., `amd64`, `arm64`). Used by `complyctl pack install` to validate host compatibility before extraction |
| `datastream` | `string` | No | Path to SCAP datastream (platform-specific) |

**Sub-entity: SystemDependency** (change `install` → `url`):

| Field | Type | Required | Description |
|:---|:---|:---|:---|
| `name` | `string` | Yes | Dependency display name |
| `kind` | `DependencyCheckKind` | Yes | Check type enum (`rpm`, `deb`, `path`) — safe, hardcoded verification |
| `value` | `string` | Yes | Value for the check (package name or path) |
| `url` | `string` | No | Documentation URL for remediation guidance. Doctor links to this on failure |

**Change from prior version**: `Install` field (freeform install command) replaced by `URL` (documentation link). Avoids assuming the user's package manager and is portable across distributions.

---

### ExampleConfig (generated)

Standard `WorkspaceConfig` YAML generated by `complypack build`. Provides a ready-to-customize starting point for pack consumers.

**Generation logic** (from `PackManifest`):

```text
policies:
  For each PackPolicyEntry:
    - url: {entry.URL}
      id: {entry.ID}

variables:
  workspace: ./.complytime/scan

targets:
  - id: local
    policies: [{all policy IDs}]
    variables: {}
```

**Source**: New — `internal/pack/example_config.go`
**Consumer**: `complypack build` generates it; `complyctl pack install` extracts it to workspace
**Format**: Standard `WorkspaceConfig` serialized via `SaveTo()` — not custom YAML generation

---

### PackInstallPaths

Resolved extraction paths for `complyctl pack install`. Two modes: workspace-local (default) and global (`-g`).

| Component | Workspace-Local (default) | Global (`-g`) |
|:---|:---|:---|
| Provider binaries | `./bin/{binary-name}` | `~/.complytime/providers/{binary-name}` |
| Policy OCI layouts | `./policies/{policy-id}/` | `~/.complytime/policies/{policy-id}/` |
| Pack manifest | `./complypack.yaml` | `./complypack.yaml` |
| Example config | `./complytime.yaml.example` | `./complytime.yaml.example` |

**Source**: New — `internal/pack/install.go`
**Note**: `complypack.yaml` and `complytime.yaml.example` always go to workspace root regardless of `-g` flag — these are user-facing files.
**Single-pack semantics**: One pack per workspace. If an existing pack is detected (by `complypack.yaml` presence), prompt for overwrite confirmation or accept `--force` flag. Second install replaces the first entirely.
**Platform validation**: Before extracting, read `platform.os`/`platform.arch` from the pack manifest layer and compare to `runtime.GOOS`/`runtime.GOARCH`. Mismatch → error with expected vs actual, no files extracted.
**Rollback**: Track all files written during extraction. On any error mid-extraction, remove all tracked files and report the error with cause. No partial installs.

---

### DoctorPackCheck (extends existing Doctor — advisory only)

Pack-layer diagnostic checks added to `complyctl doctor` when `complypack.yaml` is present. Doctor is advisory — it reports issues with actionable remediation but does not block `generate` or `scan`.

| Check | Severity | Input | Pass Condition |
|:---|:---|:---|:---|
| Pack manifest valid | Error | `complypack.yaml` | `LoadPackManifest()` + `ValidatePackManifest()` succeed |
| Provider binaries present | Error | `PackProviderEntry[]` | Each `binary` exists + executable at resolved path |
| Policy caches present | Error | `PackPolicyEntry[]` | OCI Layout dir exists at resolved path with valid `oci-layout` marker |
| System dependencies | Warn | `SystemDependency[]` | Each `kind`/`value` check passes. Failure links to dependency's `url` |

**Provider path resolution**: Check `COMPLYTIME_PROVIDER_DIR` first, then `./bin/`, then `~/.complytime/providers/`. First match wins.
**Missing config guidance**: If `complypack.yaml` present but `complytime.yaml` absent → warn: "Pack installed. Run `cp complytime.yaml.example complytime.yaml` to create config."

**Source**: Extended — `internal/doctor/doctor.go` (new `checkPack()` function)

---

## New Constants (additions to `consts.go`)

| Constant | Value | Description |
|:---|:---|:---|
| `MediaTypePackManifest` | `application/vnd.complytime.pack.manifest.v1+yaml` | Pack manifest layer |
| `MediaTypePackConfig` | `application/vnd.complytime.pack.config.v1+yaml` | Example config layer |
| `MediaTypePackProvider` | `application/vnd.complytime.pack.provider.v1` | Provider binary layer |
| `MediaTypePackPolicyLayout` | `application/vnd.complytime.pack.policy-layout.v1.tar+gzip` | Policy OCI layout layer |
| `ExampleConfigFile` | `complytime.yaml.example` | Generated example config filename |
| `PackBinDir` | `bin` | Workspace-local provider binary directory |
| `PackPoliciesDir` | `policies` | Workspace-local policy cache directory |
| `EnvProviderDir` | `COMPLYTIME_PROVIDER_DIR` | Environment variable for provider dir override |

---

## Relationships

```text
complypack build ──→ PackManifest (read complypack.yaml)
                     ├── Per-policy URL → fetch from respective registries → OCI Layout dirs (tar.gz)
                     ├── Providers → locate binaries on filesystem
                     ├── ExampleConfig → generate complytime.yaml.example
                     └── PackArtifact → assemble OCI manifest with typed layers

complypack push ──→ PackArtifact → oras.Copy() → OCI Registry

complypack pull ──→ OCI Registry → oras.Copy() → local PackArtifact

complypack doctor ──→ PackManifest (validate)
                      ├── Registries reachable? (unique registries from policy URLs)
                      ├── Policies exist remotely?
                      ├── Provider binaries exist locally?
                      └── System dependencies installed?

complyctl pack install ──→ OCI Registry → pull PackArtifact
                           ├── Check existing pack (prompt overwrite / --force)
                           ├── Validate platform.os/arch vs runtime.GOOS/GOARCH
                           ├── Extract by media type + annotation (track written files)
                           │   ├── Providers → ./bin/ or ~/.complytime/providers/
                           │   ├── Policy layouts → ./policies/ or ~/.complytime/policies/
                           │   ├── complypack.yaml → ./
                           │   └── complytime.yaml.example → ./
                           ├── chmod +x provider binaries
                           └── On error → rollback all written files

complyctl doctor (pack-aware) ──→ complypack.yaml present?
                                  ├── Yes → run pack checks + config checks
                                  │   ├── Pack: manifest valid, binaries present, caches present, sys deps
                                  │   └── Config: standard 001 checks (if complytime.yaml exists)
                                  └── No → standard 001 doctor (unchanged)

COMPLYTIME_PROVIDER_DIR ──→ ResolvePluginDir() (consts.go)
                            ├── Set → return env var value (expand ~/)
                            └── Unset → return ~/.complytime/providers/ (default)
```

## State Transitions

### Pack Build Lifecycle

```text
[complypack.yaml exists] ──(complypack doctor)──→ [Validated for buildability]
[Validated] ──(complypack build)──→ [Policies fetched, binaries located, example generated]
                                    ──→ [PackArtifact assembled]
[PackArtifact assembled] ──(complypack push)──→ [Published to registry]
[Published] ──(skopeo copy)──→ [Airgap transferred]
```

### Pack Install Lifecycle

```text
[OCI ref provided] ──(complyctl pack install)──→ [PackArtifact pulled]
[PackArtifact pulled] ──(check existing pack)──→ [Existing? prompt overwrite / --force]
[No conflict] ──(validate platform os/arch)──→ [Compatible? proceed : error + abort]
[Compatible] ──(extract layers)──→ [Workspace populated]
[Extract error] ──(rollback)──→ [All written files removed, error reported]
[Workspace populated] ──(cp example → config)──→ [complytime.yaml created]
[Config created] ──(edit targets/variables)──→ [Configured]
[Configured] ──(complyctl doctor)──→ [Diagnostics reported (advisory)]
[Ready] ──(complyctl scan)──→ [Scanning with bundled providers + cached policies]
```

### Doctor Pack Detection

```text
[complypack.yaml absent] ──→ [Standard 001 doctor: config + providers + registry]
[complypack.yaml present, complytime.yaml present] ──→ [Pack checks + config checks]
[complypack.yaml present, complytime.yaml absent] ──→ [Pack checks + warn: config missing]
```
